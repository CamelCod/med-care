{
  "name": "med-care",
  "version": "1.0.0",
  "description": "Medical Care Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "build": "echo 'Build step placeholder'",
    "docker:build": "docker build -t med-care .",
    "docker:run": "docker run -p 3000:3000 med-care",
    "docker:compose": "docker-compose up"
  },
  "keywords": ["medical", "healthcare", "nodejs", "docker"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
